# 产品需求文档（PRD）—— 产品脑暴工作台（PM Brainstorm Workbench）

---

## 修订记录

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| V1.0 | 2026-06-16 | 陈俊良 | 初始版本，基于代码库全面分析 |

---

## 1. 产品概述

### 1.1 产品定位

产品脑暴工作台是一款**AI驱动的多角色产品概念设计与压力测试工具**。面向产品经理、创业者和产品设计团队，通过多Agent协作讨论、可视化共识画布和AI压力面试官的组合，帮助用户在短时间内完成产品概念从模糊想法到结构化定义的完整闭环。

### 1.2 产品愿景

"让每一个产品想法在上线前都经过多维度、多角色的严格检验。"

### 1.3 目标用户群

| 用户画像 | 场景描述 | 核心需求 |
|----------|----------|----------|
| 产品经理（PM） | 新产品立项前进行概念验证 | 多角色视角审视、竞品分析、技术风险评估 |
| 创业者 | 验证产品想法可行性 | 商业模式验证、目标用户确认、MVP范围界定 |
| 产品设计团队 | 团队脑暴后统一认知 | 结构化讨论记录、共识与分歧可视化、产品画像生成 |
| 产品求职者 | 面试前的产品方案准备 | 产品思维训练、压力面试模拟、逻辑闭环验证 |

### 1.4 核心价值主张

**"输入一个模糊想法，收获一份经过多角色压力测试的结构化产品方案。"**

---

## 2. 产品架构

### 2.1 系统架构总览

```
┌─────────────────────────────────────────────────────┐
│                    前端层（Next.js）                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │  Landing  │  │Workbench │  │  Interview Mode   │   │
│  │  Page     │  │Chat+Canvas│ │  Voice/Text       │   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
│         │              │                  │          │
│         └──────────────┴──────────────────┘          │
│                        │ SSE 流式通信                 │
├────────────────────────┼─────────────────────────────┤
│                   API层（FastAPI）                    │
│  ┌──────┐ ┌────────┐ ┌───────┐ ┌──────┐ ┌──────┐   │
│  │Session││Brainstorm││Interview││Canvas││Voice │   │
│  │Routes ││ Routes  ││ Routes ││Routes││Routes│   │
│  └──────┘ └────────┘ └───────┘ └──────┘ └──────┘   │
│  ┌──────┐ ┌────────┐                                │
│  │Auth  │ │Recharge│                                │
│  │Routes│ │ Routes │                                │
│  └──────┘ └────────┘                                │
├─────────────────────────────────────────────────────┤
│                核心逻辑层（Core）                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐        │
│  │Agent Loop│ │Interviewer│ │Product Portrait│       │
│  │多Agent编排 │ │面试官引擎 │ │ 产品画像生成   │       │
│  └──────────┘ └──────────┘ └──────────────┘        │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐        │
│  │Canvas    │ │ LLM Client│ │   Voice       │        │
│  │Parser    │ │多Provider │ │ TTS/STT      │        │
│  └──────────┘ └──────────┘ └──────────────┘        │
│  ┌──────────┐                                        │
│  │RolePrompts│                                       │
│  │角色提示词 │                                        │
│  └──────────┘                                        │
├─────────────────────────────────────────────────────┤
│                 数据与知识层                            │
│  ┌──────┐ ┌──────────┐ ┌────────────────────┐       │
│  │RAG   │ │ Session  │ │   User/Recharge    │       │
│  │Retriever││ Store    │ │     Store          │       │
│  │(关键词)│ │ (JSON文件)│ │   (JSON文件)       │       │
│  └──────┘ └──────────┘ └────────────────────┘       │
│  ┌──────────────────────────────────────┐             │
│  │        知识库（Knowledge Base）         │             │
│  │  方法论 · 案例研究 · 框架 · 基准指标   │             │
│  └──────────────────────────────────────┘             │
└─────────────────────────────────────────────────────┘
```

### 2.2 用户流程图

```
用户输入产品想法
        │
        ▼
┌─────────────────┐
│ 产品教练（Coach） │ ◄── 通过提问帮助用户理清思路
│  · 目标用户是谁？ │     厘清用户画像、场景、问题
│  · 核心解决什么问题│
│  · 现有替代方案  │
└────────┬────────┘
         │ 确认后
         ▼
┌──────────────────────────────────────────────┐
│ 多Agent脑暴讨论（Brainstorm）                  │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────┐   │
│  │ CTO  │ │设计师 │ │运营  │ │ 目标用户  │   │
│  │技术  │ │交互  │ │增长  │ │ 真实反馈  │   │
│  │可行性│ │体验  │ │策略  │ │          │   │
│  └──────┘ └──────┘ └──────┘ └──────────┘   │
└────────┬─────────────────────────────┬──────┘
         │ 自动解析                     │ 用户可随时
         ▼                              ▼
┌──────────────────┐          ┌──────────────────┐
│ 可视化讨论画布     │          │ AI压力面试官      │
│ · 共识/分歧/总结  │          │ 6维度深度提问     │
│ · 产品画像生成    │          │ 语音面试模式      │
│ · 功能优先级      │          │ 缺口识别与报告    │
│ · 配色/线框      │          │                  │
└──────────────────┘          └──────────────────┘
         │
         ▼
┌──────────────────┐
│ 导出/分享         │
│ Markdown格式输出  │
└──────────────────┘
```

---

## 3. 功能模块详细设计

### 3.1 多Agent脑暴引擎

#### 3.1.1 角色定义

| Agent角色 | 系统名称 | 关注维度 | 核心能力 |
|-----------|---------|---------|---------|
| 产品教练 | Coach | 问题澄清 | 通过3-5个问题帮助用户理清目标用户、核心问题、现有方案、产品形态、成功标准 |
| 技术负责人 | CTO | 技术可行性 | 评估架构复杂度、可扩展性瓶颈、技术选型风险、实现成本 |
| 设计师 | Designer | 用户体验 | 评估交互流程、认知负荷、信息架构、边缘场景、无障碍性 |
| 运营负责人 | Ops | 商业可行性 | 评估市场容量、竞品格局、盈利模式、获客渠道、增长壁垒 |
| 目标用户 | User | 真实需求 | 模拟真实用户心理、质疑价值主张、评估学习成本、抵制行为改变 |
| AI面试官 | Interviewer | 压力测试 | 覆盖6个维度的尖锐提问，识别方案中的缺口和未审视的假设 |

#### 3.1.2 协作流程

```
用户输入想法
    │
    ▼
产品教练阶段 ──▶ 用户回答3-5个问题 ──▶ 教练总结确认
    │                                              │
    └──────────────────────────────────────────────┘
          用户确认后进入脑暴阶段
    │
    ▼
多Agent轮流发言（SSE流式输出，300ms内响应）
    │
    ├── CTO发言 ──▶ 自动更新画布
    ├── 设计师发言 ──▶ 自动更新画布  
    ├── 运营负责人发言 ──▶ 自动更新画布
    └── 目标用户发言 ──▶ 自动更新画布
    │
    ▼
用户可随时插话、追问或指定角色回答
    │
    ▼
进入面试阶段（可选）/ 生成产品画像（可选）
```

#### 3.1.3 关键技术设计

- **SSE流式输出**：采用Server-Sent Events实现实时流式推送，延迟控制在300ms以内
- **RAG知识增强**：基于关键词匹配的检索器，从内置知识库（方法论/案例/框架/指标）检索相关上下文注入System Prompt
- **多Agent记忆共享**：四个Agent共享同一对话历史，通过精细化System Prompt边界和各Agent内部Langgraph约束防止偏见交叉影响
- **重排策略**：粗排（关键词匹配）+ 精排（元数据加权）+ 多样性融合（MMR边际相关度），确保检索结果与Query强绑定
- **语气约束层**：所有角色共用TONE_PREAMBLE，强制"冷静、客观、直白"的评审语气，禁止赞美性回复

### 3.2 可视化讨论画布（Canvas）

#### 3.2.1 功能描述

将多Agent的脑暴讨论内容自动解析为结构化的可视化地图，包含共识、分歧和阶段性总结三种节点类型。

#### 3.2.2 数据格式

```json
{
  "topic": "问题陈述一句话",
  "timeline": [
    {
      "id": "c1",
      "type": "consensus",       // 共识
      "content": "内容概述",
      "roles": ["cto", "designer"]
    },
    {
      "id": "d1",
      "type": "disagreement",    // 分歧
      "content": "分歧内容",
      "roles": ["cto", "user"],
      "positions": [
        {"role": "cto", "stance": "技术立场"},
        {"role": "user", "stance": "用户立场"}
      ]
    },
    {
      "id": "s1",
      "type": "summary",         // 阶段性总结
      "content": "阶段成果概述",
      "roles": ["cto", "designer", "ops", "user"]
    }
  ]
}
```

#### 3.2.3 更新机制

- **自动增量更新**：每轮Agent发言完成后，前端自动调用canvas/incremental接口增量更新画布
- **画布缓存合并**：后端维护已有画布数据，将新对话与现有timeline合并，保持节点顺序
- **前端输入**：用户可随时手动刷新/重新生成画布

### 3.3 产品画像生成（Product Portrait）

#### 3.3.1 功能描述

基于脑暴对话内容，由LLM自动生成结构化的产品画像，包含产品名称、价值主张、核心功能、配色方案、交互风格和页面线框描述。

#### 3.3.2 输出数据

```json
{
  "product_name": "产品名称（2-6字）",
  "tagline": "一句话核心价值（20字内）",
  "target_users": "目标用户描述（含场景）",
  "core_features": [
    {"name": "功能名", "description": "描述", "priority": "must-have"}
  ],
  "style_keywords": ["极简", "年轻化"],
  "color_scheme": {
    "primary": "#FF6B35",
    "secondary": "#F7C59F",
    "accent": "#2EC4B6",
    "background": "#FFFFFF"
  },
  "interaction_style": "主要交互方式",
  "wireframe_description": "用[header][nav][content][sidebar][footer]标记的布局描述"
}
```

### 3.4 AI压力面试官

#### 3.4.1 功能描述

在产品方案初步形成后，AI面试官以冷淡、客观的风格，对产品方案覆盖全部6个维度进行深度压力测试，识别出方案缺口和未审视的假设。

#### 3.4.2 六维提问体系

| 维度 | 关键词 | 考察重点 |
|------|--------|---------|
| 问题有效性 | 问题、证据、痛点、假设 | 这是真问题还是伪需求？有什么证据？ |
| 方案有效性 | 解决、根因、核心功能 | 方案解决了根本原因还是表面症状？ |
| 技术风险 | 技术、架构、依赖、故障 | 最难的技术点是什么？第三方依赖风险？ |
| 商业可行性 | 付费、收入、竞品、壁垒 | 谁付钱？单位经济模型？竞争壁垒？ |
| 用户采用 | 留存、激活、切换、习惯 | 用户为什么要改变现有行为？激活时刻？ |
| 执行风险 | 上线、资源、范围、优先级 | 砍掉50%功能砍哪些？谁会反对？ |

#### 3.4.3 面试流程控制

- **起始**：从6个维度中尚未覆盖的维度开始提问，循环覆盖
- **迭代**：用户每次回答后，面试官追问下一个维度的问题
- **结束条件**：全部6个维度覆盖完成 + 问题数量 >= 10，输出缺口报告
- **维度追踪**：后端自动记录已覆盖维度列表，指导下一步提问方向
- **语音模式**：支持TTS播报面试官问题 + STT语音回答

### 3.5 语音交互系统

#### 3.5.1 技术实现

| 功能 | 技术方案 | 说明 |
|------|---------|------|
| TTS（文字转语音） | edge-tts | 微软Edge TTS引擎，支持多种中文语音（默认：zh-CN-YunjianNeural；面试官：zh-CN-YunxiNeural） |
| STT（语音转文字） | HuggingFace Whisper（首选）+ OpenAI Whisper（备选） | 优先使用HF免费服务，回退到OpenAI兼容API |
| 浏览器录音 | MediaRecorder API（WebM格式） | 前端录音后上传服务端识别 |

#### 3.5.2 面试语音模式流程

```
用户点击语音模式
    │
    ▼
面试官提问 ──▶ TTS自动播报（edge-tts）
    │
    ▼
用户语音回答 ──▶ 浏览器录音 ──▶ 上传WebM
    │
    ▼
STT识别 ──▶ 文字回填输入框 ──▶ 自动发送
    │
    ▼
面试官追问下一轮
```

### 3.6 知识库系统（RAG）

#### 3.6.1 知识库结构

```
rag/knowledge/
├── methodologies/          # 产品方法论
│   ├── jtbd.md             # Jobs-to-be-Done
│   ├── kano_model.md       # 狩野模型
│   ├── lean_canvas.md      # 精益画布
│   └── rice.md             # RICE优先级框架
├── case_studies/           # 案例研究
│   ├── instagram_pivot.md  # Instagram从Burbn转型
│   ├── slack_plg.md        # Slack产品驱动增长
│   └── tiktok_algorithm.md # TikTok推荐系统
├── frameworks/             # 思维框架
│   ├── first_principles.md # 第一性原理
│   └── premortem.md        # 事前验尸
└── benchmarks/             # 行业基准
    └── saas_metrics.md     # SaaS关键指标
```

#### 3.6.2 检索机制

- **检索方式**：关键词匹配（Tokenize + 词频评分），不依赖Embedding服务，轻量级部署
- **评分加权**：基础关键词匹配分数 + 分类匹配加分（+2）+ 标题匹配加分（+3）
- **上下文注入**：检索结果以`## 相关知识库参考`区块注入每个Agent的System Prompt
- **动态加载**：懒加载机制，首次搜索时从`knowledge_base.json`加载索引

### 3.7 BYOK（Bring Your Own Key）多Provider支持

#### 3.7.1 支持的LLM Provider

| Provider | 自动检测前缀 |
|----------|-------------|
| DeepSeek | sk-a6f, dz- |
| OpenAI | sk- |
| Anthropic | sk-ant- |
| Moonshot | moonshot- |
| 通义千问 | sk-dashscope- |
| 智谱GLM | 无前缀 |
| 其他（零一、MiniMax、百川、讯飞、火山、腾讯混元、SiliconFlow、Groq、Together等） | 用户手动配置 |

#### 3.7.2 安全约束

- **域名白名单**：仅允许18个已知LLM提供商的API域名，防止SSRF攻击
- **Base URL校验**：所有自定义Base URL必须匹配白名单域名或子域名

### 3.8 用户与计费系统

#### 3.8.1 用户体系

| 功能 | 说明 |
|------|------|
| 匿名用户 | 通过X-User-Token标识，自动创建 |
| 手机号登录 | SMS验证码登录（支持阿里云短信/调试模式） |
| JWT认证 | 72小时有效期，HS256签名 |
| 会话隔离 | 用户仅能访问自己的会话 |

#### 3.8.2 计费模型

| 套餐 | 额度 | 价格 |
|------|------|------|
| 标准版 | 200,000 tokens | ¥10 |
| 专业版 | 500,000 tokens | ¥30 |
| 旗舰版 | 1,000,000 tokens | ¥50 |

**充值流程**：用户提交充值请求 → 获取验证码 → 确认付款 → 管理员审核 → 额度到账

**BYOK模式**：用户使用自己的API Key时不消耗平台额度

---

## 4. 用户界面设计

### 4.1 页面结构

| 页面 | 路由 | 功能描述 |
|------|------|---------|
| 登录页 | /login | 手机号验证码登录 |
| 首页 | / | 输入产品想法，创建新会话 |
| 脑暴工作台 | /session/[id] | 左侧画布面板 + 右侧聊天面板 |
| 面试模式 | /session/[id]/interview | 全屏AI压力面试视图 |

### 4.2 首页设计

- **品牌标识**：PM Brainstorm Workbench + 暖色渐变Logo
- **角色展示**：CTO、设计师、运营、目标用户四个角色图标和简介
- **输入区域**：textarea + 4个Prompt模板快捷填充
- **功能亮点**：多角色圆桌 / 可视化画布 / 压力测试三个卡片

### 4.3 脑暴工作台设计

**布局**：左右分栏（桌面端）/ 上下切换（移动端）

**左侧画布面板（CanvasPanel）**：
- 讨论地图视图：时间线形式展示共识、分歧、总结节点，交替左右布局
- 产品画像视图：卡片式展示产品名称、目标用户、核心功能、配色方案、页面线框
- 画布工具栏：刷新、导出等操作

**右侧聊天面板（ChatPanel）**：
- 顶部Header：阶段标识、消息计数、画像生成/导出/充值/设置按钮
- 消息列表：角色头像 + 角色名 + 对话气泡，角色名称颜色标识
- 角色选择器：底部横向滚动，可选择单个角色提问或@全体
- 输入框：支持文字输入 + 语音输入 + 发送快捷键

### 4.4 面试模式设计

- **面试Header**：面试官头像 + 维度覆盖进度 + 语音/文字模式切换
- **面试消息**：面试官左侧气泡（红色标识）+ 用户右侧气泡（琥珀色标识）
- **语音模式**：自动TTS播报 + 录音按钮 + 实时转写
- **输入框**：文字输入 + 语音输入

### 4.5 视觉设计规范

| 元素 | 规范 |
|------|------|
| 主色调 | 琥珀色系（amber-50 ~ amber-900） |
| 角色配色 | CTO蓝色(#3b82f6)、设计师紫色(#a855f7)、运营绿色(#22c55e)、用户橙色(#f97316)、面试官红色(#ef4444) |
| 背景 | 温暖米色（warm-50） |
| 圆角 | 中大型圆角（rounded-xl, rounded-2xl） |
| 阴影 | 轻阴影（shadow-sm, shadow-md） |
| 动效 | 淡入上移（fade-in + translate）、脉冲动画（pulse）、弹性过渡 |

---

## 5. 数据模型

### 5.1 Session（会话）

```json
{
  "id": "12位hex",
  "problem_statement": "产品想法描述",
  "phase": "coach | brainstorm | interview",
  "messages": [
    {
      "role": "user | assistant",
      "content": "消息内容",
      "role_name": "cto | designer | ops | user | coach | interviewer",
      "timestamp": "ISO日期"
    }
  ],
  "discussion_map": { "topic": "", "timeline": [] },
  "product_portrait": {},
  "interview_dimensions_covered": [],
  "interview_question_count": 0,
  "created_at": "ISO日期",
  "user_token": "用户标识"
}
```

### 5.2 User（用户）

```json
{
  "user_token": "唯一标识",
  "phone": "手机号",
  "nickname": "昵称",
  "api_key": "用户自带Key",
  "base_url": "自定义地址",
  "model": "自定义模型",
  "token_quota": 100000,
  "tokens_used": 0,
  "created_at": "ISO日期"
}
```

---

## 6. 技术约束与风险

### 6.1 部署约束

- **后端**：Python FastAPI + Uvicorn，JSON文件存储（无需数据库）
- **前端**：Next.js静态导出 + FastAPI静态文件托管，单体部署
- **容器化**：提供Dockerfile，支持一键Docker部署
- **环境变量**：LLM_API_KEY、LLM_BASE_URL、LLM_MODEL、ADMIN_TOKEN、JWT_SECRET、HF_API_TOKEN等

### 6.2 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| LLM API Key未配置 | 服务不可用 | BYOK模式 + 详细诊断信息 + 前端引导配置 |
| JSON文件并发写入 | 数据一致性问题 | 单进程部署 + 文件级锁定（当前仅适用于轻量单实例） |
| HuggingFace Whisper不可用 | 语音识别失败 | 自动回退到OpenAI Whisper |
| Agent输出质量不稳定 | 产品分析质量波动 | 语气约束+知识增强+重排策略+反幻觉Prompt |
| SEO | 无法被搜索引擎索引 | 纯客户端渲染，SEO非核心场景 |

### 6.3 未来优化方向

| 方向 | 优先级 | 说明 |
|------|--------|------|
| 数据库迁移 | P1 | 从JSON文件迁移到SQLite/PostgreSQL，支持并发写 |
| 嵌入向量检索 | P2 | 从关键词检索升级到Embedding语义检索，提升RAG质量 |
| 更多Agent角色 | P2 | 增加数据分析师、增长黑客、法务等角色 |
| 多人协作 | P3 | 支持多用户实时协作脑暴 |
| 用户反馈标注 | P3 | 用户可以对Agent输出进行好评/差评，形成反馈闭环 |
| API文档自动生成 | P1 | 集成OpenAPI/Swagger UI |

---

## 7. 功能优先级与路线图

### Priority 0（已实现 · 核心能力）

| 功能 | 说明 |
|------|------|
| 多Agent脑暴讨论 | CTO/设计师/运营/目标用户 四角色轮流发言 |
| 产品教练引导 | 用户输入想法后三步澄清流程 |
| SSE流式消息 | 实时流式输出，角色切换动画 |
| RAG知识库 | 内置方法论/案例/框架/指标知识库 |
| 可视化讨论画布 | 共识/分歧/总结时间线展示 |
| AI压力面试 | 6维度问题覆盖，对话式追问 |
| 音色语音面试 | edge-tts TTS + Whisper STT |
| 产品画像生成 | 自动生成产品名称/功能/配色/线框 |
| BYOK多Provider | 18+ LLM提供商支持 |
| 会话管理 | 创建/加载/历史列表 |
| Markdown导出 | 会话 + 画布一键导出 |

### Priority 1（已实现 · 增强体验）

| 功能 | 说明 |
|------|------|
| 手机号登录 + SMS验证 | 短信验证码登录 |
| JWT认证 | 72小时Token有效期 |
| Token计费系统 | 三种套餐 + 管理员审核 |
| 移动端响应式布局 | 画布/聊天面板切换 |
| 可视化额度管理 | Header实时显示剩余额度 |
| Prompt模板快捷填充 | 4个预设Prompt模板 |
| 引导式新手教程 | Onboarding Modal首次使用引导 |

### Priority 2（已实现 · 工程基建）

| 功能 | 说明 |
|------|------|
| 限流中间件 | 每用户每60秒20次请求限制 |
| 健康检查 + API信息 | /health + /api-info |
| 连接诊断接口 | /api/user/diagnose 详细连接诊断 |
| 重连机制 | SSE 5次指数退避重连 |
| 错误处理 | 友好的中文错误提示 |
| API Key自动检测 | 根据Key前缀自动匹配Provider |

---

## 8. 关键数据指标

| 指标 | 定义 | 目标 |
|------|------|------|
| 脑暴完成率 | 用户从创建到进入面试的比例 | > 40% |
| 平均脑暴时长 | 从创建到完成/退出的平均时长 | > 10分钟 |
| 画布生成率 | 成功生成讨论画布的会话比例 | > 60% |
| 面试完成率 | 完成全部6维度的面试比例 | > 30% |
| 产品画像生成率 | 用户点击生成画像的比例 | > 20% |
| 用户次日留存 | 次日再次使用的用户比例 | > 15% |
| BYOK采用率 | 使用自有API Key的用户比例 | > 10% |

---

